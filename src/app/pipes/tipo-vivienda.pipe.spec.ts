import { TipoViviendaPipe } from './tipo-vivienda.pipe';

describe('TipoViviendaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoViviendaPipe();
    expect(pipe).toBeTruthy();
  });
});
